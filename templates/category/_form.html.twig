{{ form_start(form) }}
 <div class="col-6">
        <div class="mb-3">
    {{ form_widget(form.name,{
            'attr': {
                'placeholder': 'Nom du produit',
            'class': 'form-control'
            },
            }) }}
</div></div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
